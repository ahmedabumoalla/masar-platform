# Masar — current task

Updated 2026-09-23, workspace C:/Projects/masar; installation request feature verified on a Vercel preview, GitHub/production publication in progress

## Latest change — device installation requests
- Home hero now has a dedicated installation CTA and a responsive native RTL dialog with seven required fields: applicant name/email/mobile, Google Maps link, device quantity, farm area in square metres and plants. Keyboard/focus restoration, internal scrolling, 16px inputs, 44px controls, inline validation/error summary, pending guard and success confirmation are source-reviewed
- Real `POST /api/installation-requests` persists records in private Vercel Blob store `masar-installation-requests` connected only to this project's Production/Preview. Success follows durable storage; duplicate retries preserve the original object; personal data is never stored in localStorage, returned in responses or logged. Details/operator access: `docs/installation-requests.md`
- Server validates all fields and request origin/content type, caps body size, rejects a honeypot and provides bounded per-instance throttling. This is not a distributed rate limit. No email recipient was supplied; records are available in private Vercel Storage, with no email notifications
- RUN 2026-09-23 local Node 20.20.2: `npm test` 27/27 pass (444.8573 ms), then `npm run build` strict TypeScript + Vite pass. Lockfile now includes @vercel/blob 2.8.0; npm audit during install reported 0 vulnerabilities. The pre-existing Three.js chunk-size warning remains
- An initial preview passed its build but its function failed to resolve .ts imports after deployment. Corrected server imports to .js specifiers. RUN affected `npm run test:installation` 8/8 pass (277.592 ms) and `npx tsc -b` pass. Unchanged frontend build and remaining 19 tests remain valid
- RUN final preview build on Vercel Node 24, deployment `dpl_46GXzHj2JrsXM9dmpAuydkMUNrF9`, READY. Preview POST returned 201 success at 2026-09-23 04:06 +03:00. Authenticated Blob read verified all seven synthetic fields; anonymous read returned 403 at 04:06:47 +03:00. No real customer data was used
- Preview: https://masar-platform-mbrf7pjtf-ahmedabumoallas-projects.vercel.app. NOT RUN browser rendering/interaction; UI review was source-based. Production verification and exact synthetic-record cleanup remain pending publication
- CLI authentication is now available for GitHub ahmedabumoalla and Vercel ahmedabumoallas-projects. Git working checkout is `.tools/release-checkout`; the root still has no .git. Local `.env.local`, `.vercel` and `.tools` contain excluded tooling/configuration and must never be uploaded to Git

## Latest operation — publish current project to GitHub
- User explicitly authorized replacing the old remote project with the current local Masar platform through their authenticated browser. Main updated without force-push, preserving prior history: https://github.com/ahmedabumoalla/masar-platform/commit/eb28187c053ce0ad9bf0d3cb4a8facb0458eab12
- Published 61 reviewed files including React/Vite source, interactive atlas, full-screen simulation, original brand assets, editable Blender scenes, exported GLBs and reproduction scripts. Excluded dependencies, build output, local tools, logs, reference PDF images and secrets. Archive SHA-256: 9311be88042efecdaa9ee7952ab389fbf7e58ffb20e2c296a86fc22614c4081c
- Release preparation: vercel.json explicitly selects Vite, npm ci and dist, and excludes data/ from the SPA rewrite; .gitignore excludes local tools and Python caches; README includes the atlas. No app behavior was changed during publishing
- RUN 2026-09-22 00:06 +03:00 GitHub Actions on ubuntu-latest, application Node 20.20.2: archive identity/path checks, npm ci, npm test, npm run build, whitespace check and fast-forward push passed. Run duration 27s (job 24s): https://github.com/ahmedabumoalla/masar-platform/actions/runs/35655150902
- Earlier run 35654923108 passed install/tests/build but stopped before committing because Git treated Windows CRLF as trailing whitespace. Resolved by enabling cr-at-eol in the whitespace check, preserving the exact source bytes; final run passed. GitHub emitted a nonfatal Node 20 action-runtime deprecation warning
- Browser file-picker automation remained blocked; user manually selected the reviewed archive. Archive and one-shot publishing workflow remain on codex/current-masar-20260921 only, not main. Workflow refuses unexpected archive content or a changed main baseline
- NOT VERIFIED: current public Vercel deployment or new live-site/browser application behavior. GitHub success is not deployment evidence. This publication record and handoff note were written locally after the published source snapshot

## Latest change — full-width atlas and popup details
- Removed the permanent atlas sidebar; the map now spans the section width on desktop/mobile. Region, governorate and search controls occupy a compact toolbar above it
- Marker click, Enter/Space, native place selection and search result selection open `AtlasPlaceDialog` containing the same location, water/crop evidence, sources and simulation link. Arrow-key exploration moves selection without repeatedly opening dialogs. Drag gestures do not open details
- Native modal provides background inertness/focus containment, focused 44px close control, Escape, scroll locking/restoration, opener focus restoration and internally scrolling content bounded by dynamic viewport height. Source-reviewed responsive toolbar and modal; no browser opened
- RUN 2026-09-21 23:31:01 +03:00 `npm run build`, strict TypeScript + Vite exit 0; `docs/reference/build-atlas-dialog.log`, existing nonfatal Three.js chunk warning remains
- RUN focused Node/tsx React server-render assertions for Ahsa/local and Khobar/regional dialog: title, water/crops, sources, simulation link and regional evidence note pass. This checks data/markup wiring, not browser focus or rendered layout
- REUSED atlas 5/5 tests from 23:25:10 after comparing unchanged module/test/content/geographic/public-license/dependency/config SHA-256. No data, camera math, engine or dependency changes. NOT RUN browser/native modal interaction or physical device review

## Latest change — Saudi water and agriculture atlas
- Home now includes a lazy interactive national SVG atlas after the hero: 13 regions, 152 representative governorate/capital locations, Arabic search, region/native place selectors, circular water-context markers, zoom/pan/reset, directional keyboard navigation and responsive map/profile order. Original simulation/device behavior unchanged
- `src/lib/saudi-agriculture.ts`: 13 regional profiles plus 5 independently sourced local profiles (Ahsa, Taif, AlUla, Bishah, Ad Dair). Ahsa dates and Hasawi lime confirmed. Unknown local detail uses conspicuous regional scope rather than fabricated county rankings. Water/crop source links, dates, geographic limitations and attribution included; water colors are context, not measured dominant shares
- Geography: geoBoundaries ADM2 2021 county outlines dissolved into 13 regions; separate ADM1 2017 national silhouette fills coverage gaps. Initial mixed-vintage containment failed for three points; resolved by deriving region outlines from county data instead of moving them to incorrect regions. All final 152 point-containment assertions pass. Added Al Bayda official emirate coordinate and Al Amwah GeoNames coordinate. Adapted JSON and separate licenses published under public/data; offline generator uses local `.tools/atlas-python` Shapely 2.1.2, no new npm/runtime dependency
- RUN 2026-09-21 23:25:10 +03:00 `npm run test:atlas`: 5/5 pass, 229.0942 ms, Node 20.20.2; `docs/reference/test-saudi-atlas.log`. Checks cover scope/coverage/sources, Arabic search/filter/no-results, complete portrait/desktop framing, zoom/keyboard navigation and public/source data equality. Final run includes revised geographic inputs
- RUN 2026-09-21 23:25:10 +03:00 `npm run build`: strict TypeScript + Vite exit 0; `docs/reference/build-saudi-atlas.log`. Existing nonfatal Three.js chunk-size warning remains (PowerShell labels stderr NativeCommandError, process exit remains zero). Earlier intermediate build superseded by final geometry revision
- REUSED 14/14 simulation/report tests from 20:53:06 after matching both logic/test modules, package-lock and tsconfig SHA-256 plus Node version. package.json only extends the test file list and adds a focused atlas command; engine and report inputs unchanged
- Source UI/security review: 44px controls, native selectable alternative to dense points, SVG button semantics and roving focus, Arabic RTL, mobile order, local assets, source HTTPS links, no sensitive data/API keys/external writes. NOT RUN: browser rendering/touch inspection, physical mobile device, deployment, fresh dependency audit. Fullscreen browser evidence remains applicable only to the unchanged simulation, not this new atlas
- Details/reproduction: `docs/saudi-atlas.md`; current input identities in `docs/reference/verification-inputs.json`

## Latest change — full-screen map and accessible action dock
- `/simulation` now uses the entire dynamic viewport, without the old page header, framed farm card, side columns or page scrolling. Extracted presentation to `src/components/Simulation.tsx` with dedicated CSS; Home and Device keep their existing layout
- Floating brand/back controls, compact status/alarm and edge camera controls surround the canvas. Bottom dock exposes play/pause, leak/isolate/repair and four labelled entries for experiment, readings, view and report. Native dialog panels contain sector/severity/pump/speed/reset/help, telemetry/stages, layers/camera/readout toggle, and latest/empty report
- Alarm still reveals/tracks the damaged buried branch and now focuses the actual keyboard-controllable 3D region without scrolling. Repair still opens the report automatically. Modal Escape/focus restoration and report reopening remain available. Full-screen page alone owns scroll locking, avoiding stale locks when navigating away
- Responsive camera FOV preserves horizontal framing on narrow full-height canvases. Markers stay outside header/dock areas; sensor cards can be hidden through View. `viewport-fit=cover`, safe-area insets, `100dvh`, small-landscape layout and minimum 44px primary/camera/dock controls implemented
- RUN 2026-09-21 22:45:02 +03:00 `npm run build`, strict TypeScript + Vite, exit 0; `docs/reference/build-fullscreen-lab.log`. Final rebuild includes focus, short-landscape marker clearances and touch target refinements. Existing nonfatal Three.js chunk-size warning remains
- RUN focused Chrome WebGL/UI inspection 22:39–22:44 +03:00: canvas/page exactly 390x844 and 1440x1000; page exactly 844x390 and 568x320 in landscape. No page overflow; all dock actions inside viewport. Main action -> B alarm -> cutaway/focus -> isolate -> repair -> modal report works without page scroll (scrollX/Y both zero). Experiment, telemetry and view panels inspected; top camera and hide-readouts controls work. Report/telemetry close and Escape checked; navigation to Device restores body overflow. No JS errors observed
- REUSED 14/14 engine/report tests from 20:53:06: current SHA-256 comparison confirmed both logic modules, both tests and package-lock unchanged. No engine/dependency/asset changes; no redundant rerun. Source review covers keyboard controls, native modal focus/inertness, responsive layout, reduced motion, disabled actions and report empty state
- NOT RUN: physical mobile device, other browser engines, live hardware, public deployment, fresh dependency audit. Latest UI evidence supersedes previous framed-layout reviews; identities in `docs/reference/verification-inputs.json`

## Latest change — leak alarm and repair report
- Fixed top-screen alarm appears on a leak, changes from analysis to detected sector, and links to the farm region with keyboard focus, scroll, camera tracking and automatic buried-pipe reveal. Isolation changes the banner to awaiting repair; repair/reset clears it
- Repair now requires isolation and creates an immutable per-incident report before clearing the fault. Captures incident-only water loss, detection/isolation/repair elapsed simulation times, flowing duration, mean/peak leak flow and minimum pressure while leaking. Network cumulative loss remains separate; latest completed report persists across route changes in memory and can be reopened
- Accessible native modal opens after repair, traps focus, supports Escape/close, restores focus to the latest-report button and restores body scrolling. Responsive report includes hypothetical savings for 1/6/24 hours and editable cost (default 5 SAR/m3 explicitly illustrative, not an official tariff). Savings = average active leak L/min x future minutes / 1000 x price, distinct from measured simulated loss
- RUN 2026-09-21 20:53:06 +03:00 `npm test`, 14/14 pass, 220.9154 ms, Node 20.20.2. Existing 9 engine cases plus 5 report cases: multiple incidents, variable severity, stopped pump/pause/isolation, relative timestamps, units, invalid assumptions and repeated repair/reset. Log `docs/reference/test-incident-report.log`. Initial Windows wildcard invocation failed before tests; package script now names both files explicitly and succeeds
- RUN 2026-09-21 20:52:54 +03:00 `npm run build` (strict TypeScript + Vite), exit 0; `docs/reference/build-incident-report.log`. Subsequent package.json change only fixes test invocation and does not alter build inputs. Existing Three.js chunk warning remains
- RUN focused Chrome flow 20:53–20:55 +03:00: immediate alarm, B localization, click alarm from surface mode -> cutaway + tracking + farm-region focus, isolate -> repair -> native modal. Example measured incident loss 3.68 L stays fixed while assumptions change projected savings from 576 L / 2.88 SAR (1h, 5 SAR/m3) to 3456 L / 34.56 SAR (6h, 10 SAR/m3). Latest report reopens unchanged; Escape restores focus/scroll. Desktop 1440x1000 and mobile 390x844 report inspected, no horizontal overflow or JS errors
- Source review: reduced-motion scroll/camera respected, alert text changes only with phase rather than announcing live numeric counters, original material/device/farm geometry unchanged, all calculations local with no secrets or external writes. No new dependencies
- NOT RUN: physical sensor integration, calibrated tariff/hydraulics, deployment, fresh dependency audit (unchanged lockfile), unrelated device browser checks. Final identities in `docs/reference/verification-inputs.json`; older engine/build evidence superseded for changed files

## Latest change — upward opening and orange component selection
- User clarified that selection must tint the actual 3D component orange. Every primitive now receives a translucent orange overlay only while its part is selected; original materials remain intact. Sidebar selection stays yellow
- Blender explosion now rotates the housing and all components -pi/2 around X: opening faces world +Y, parts align over its X/Z centre. Camera elevated to see into the cavity. Added projected clearance so the lid, gasket and PCB do not obscure one another
- Rebuilt editable blend/GLB with Blender 4.5.3 `--background --python-exit-code 1 --python scripts/build_device.py -- --skip-render`; log `docs/reference/build-device-upward.log`. Assembled preview and farm geometry unchanged. GLB 2,149,160 bytes, URL cache revision 3, transform protocol remains layoutVersion 2
- RUN 2026-09-21 20:44:21 +03:00 actual Three.js GLTFLoader assertions: 8 assembled/exploded bounds match within 0.00002, opening normal +Y, aligned centres, all seven gaps >=0.34. See latest exportVerification in `docs/reference/device-layout-verification.json`; supersedes earlier fixed-gap evidence
- RUN 2026-09-21 20:45:14 +03:00 `npm run build`, exit 0, strict TypeScript + Vite, Node 20.20.2; `docs/reference/build-device-upward-web.log`. Final rebuild follows revised GLB spacing; existing nonfatal chunk-size warning remains
- RUN focused browser inspection 20:43–20:45 +03:00: upward cavity and separated stack, orange PCB/display/gasket/housing, previous part returns to original material, 1440x1000 desktop and 390x844 mobile. No horizontal overflow. Browser necessary to judge cavity visibility, projected occlusion and selection-layer contrast
- REUSED previous engine test evidence after confirming unchanged engine/test/lockfile hashes. No fresh farm scenario suite, calibration, dependency audit or public deployment; this change only affects device presentation and the asset cache URL

## Latest refinement — device fit and simulation
- User requested a higher-quality simulation and correction of components intersecting the housing
- Device rebuilt in Blender with separate internal depth layers, own-centre pivots and geometry-derived exploded offsets. Export v2 has 8 groups, 33 primitives, 2,149,040 bytes. Full exploded height 11.70 units, all seven adjacent clearances 0.34; editable blend/source/preview updated
- Viewer now uses assembledOrigin + explode × progress and per-part rotations; camera frames actual metadata bounds. Added isolated component inspection, responsive framing and reset-to-current-view
- Farm now has a local trench with soil strata instead of globally transparent terrain, pressure/severity-driven side jets, contained underground leakage, wet patches, branch valve wheels, flow tracers with separate inlet/outlet velocities, sensor pulses, projected live readings and smooth sector tracking
- Source/UX review corrected track-button width, elevated the focus camera to see into the trench, and clears tracking state on manual camera movement. No backend, credentials, dependencies or simulation-engine changes
- RUN Blender and actual Three.js exported-vertex validation: see `docs/reference/device-layout-verification.json` actual timestamps and bounds, all 8 roots agree within 0.00002, all gaps pass. Earlier fixed-offset device evidence superseded
- RUN targeted trench half-space and underground-jet boundary assertions by farm worker, pass. Initial inline TS-in-Node invocation failed syntactically; corrected JS invocation passed. No unresolved assertion failure
- RUN 2026-09-21 16:58:51 +03:00: final `npm run build`, strict TypeScript + Vite, exit 0, Node 20.20.2; log `docs/reference/build-refinement.log`. Rebuilt once after final CSS control-width correction to refresh dist. Nonfatal Three.js chunk-size warning remains
- REUSED engine 9/9 test result from original ~15:49 execution; SHA-256 comparison at 16:52 confirmed unchanged engine, test, lockfile and config inputs, same Node runtime. No dependency installation or engine retest in this refinement
- RUN focused Chrome WebGL review 16:52–16:58 +03:00, necessary to observe part intersections, clipped terrain and projected markers. Device assembled/exploded/isolated PCB viewed; desktop 1440×1000 and mobile 390×844. Final exploded stack has visible gaps and fits mobile. No horizontal overflow
- RUN B leak detection and isolation: inlet/outlet become 0/0 after isolate; underground jet and surrounding solid terrain visually checked from elevated tracking view. Surface mode hides B sensor pins. C surface jet/detection checked. Pause held water loss at 1.82 L across observations. Reset, sector selection and focus controls exercised. Mobile readouts stayed within scene; no JS errors observed
- NOT RUN anew: full browser suite, separate ESLint (not configured), fresh dependency audit (lockfile unchanged), physical device integration/calibration, public deployment. Geometry remains a reference-based reconstruction, detection still identifies the branch between sensors
- Current changed input identities are recorded in `docs/reference/verification-inputs.json`; previous build/browser results below are historical and superseded for changed viewers/assets

## Scope and sources
- User requests Optivio-inspired Arabic platform, original Masar identity, a faithful Blender device with internals, live farm irrigation/leak simulation including surface and buried pipes
- Reviewed https://optivio-6isx.vercel.app/ar and /ar/simulation in browser by explicit user request: focused device landing, immersive center 3D scene, side information, layer/camera controls and bottom step timeline
- PDF supplied locally: 22 pages; page 1 original logo/tagline; page 6 assembled/exploded meter; page 9 illustrative pressure/flow readings
- PDF identity navy/blue overrides copied Seet red rule with latest user request; AGENTS.md corrected
- Image-derived geometry only, no engineering dimensions or validated detection accuracy in source; simulation must be clearly illustrative

## Implemented
- Stack: React 19.3 + TypeScript 5.9.3 + Vite 8.3, Three.js 0.186, Blender 4.5.3 LTS asset exports, Node 20.20.2 on Windows
- Skills: codex-token-efficiency, frontend-design, ui-ux-pro-max, ECC frontend-design-direction/react-patterns; browser for requested reference review
- `/`: original PDF logo, exact tagline and interactive instrument; `/device`: assembled/exploded 8 component groups; `/simulation`: farm, six mounted meters, three branches and surface/buried layers
- Leak scenario changes pressure/flow, detects branch from sensor imbalance after 3 simulated seconds, shows water particles/arcs and highlighted pipe, isolates damaged branch, repairs and resets
- Pause, pump switch, severity, speed, top/orbit camera, zoom and keyboard controls; mobile scene framing fixed; reduced motion handled
- Editable sources: `assets/blender/masar-device.blend` and `masar-farm.blend`; deterministic builders in scripts; latest GLBs 2.15 MB and 1.09 MB
- Focus management, per-part material highlighting and GPU disposal reviewed and corrected; farm omits hidden internals from rendering
- Source images extracted under docs/reference, original PDF kept outside web assets
- Local development preview: http://127.0.0.1:5188 — hidden node process started in this workspace; startup command in README; port 4173 belonged to another app and was not changed
- `vercel.json` prepared for static hosting; no external services, credentials, authentication or sensor connection

## Checks
- 2026-09-21: reference PDF decoded with PyMuPDF, 22 pages rendered, device and identity visually inspected
- RUN 2026-09-21 ~15:49 +03:00: local `tsx --test src/lib/simulation.test.ts` by delegated engine worker, 9/9 pass, 278 ms, Node 20.20.2. Tests cover all three localization cases, conservation, timing, loss integration, pump interruptions, isolation, pause/speed, invalid numeric input and repair/reset. Engine/test files have not changed since that run; REUSED after UI-only changes, no redundant rerun
- RUN 2026-09-21: both `blender --background --python scripts/build_*.py` builds/export/renders passed in Blender 4.5.3. Agent visual render and GLB checks: 8 device roots/33 primitives; 6 farm roots/32 meshes/20,442 triangles, self-contained assets. Asset inputs unchanged since validation; see respective model docs and `docs/reference/farm-metadata.json`
- RUN 2026-09-21 16:15:15 +03:00 final build: `npm run build` (strict TypeScript + Vite), exit 0. Exact execution time is file timestamp on `docs/reference/build-final.log`. Final build includes camera fit, material/disposal fixes, spray/highlight effects, font subsets and contrast changes. Current unchanged inputs captured in `docs/reference/verification-inputs.json`; this is a post-build identity record, not a retroactively invented unit-test run fingerprint
- Vite reports a non-fatal large chunk warning for lazily loaded Three.js (~652 kB minified / 163 kB gzip). No build errors remain. Initial Vite 8 manualChunks object incompatibility resolved with a function
- RUN 2026-09-21 16:03–16:15 +03:00: focused Chrome WebGL inspection because source/unit tests cannot resolve GLB rendering, pipe alignment and narrow-screen camera fit. Inspected assembled/exploded device, desktop farm and buried cutaway; triggered/detected A, B and C, isolated B (B 0/0 while A 40/40 and C 36/36), confirmed repair, pause, reset. Visual spray/highlight verified on C. UI inspected at 1440×1000 and 390×844; no horizontal overflow, narrow device clipping fixed and rechecked. No JavaScript error logs observed. Temporary viewport restored, home tab left open
- RUN 2026-09-21: key text contrast checked mathematically. Primary blue/white 4.98:1; final healthy-state green and muted-on-tinted-paper also exceed 4.5:1. Source review covers labels, selected states, keyboard camera alternatives, visible focus, touch targets, reduced motion and loading/error fallbacks
- RUN 2026-09-21 `npm install`: npm audit 0 vulnerabilities for installed lockfile, unchanged since run. Source/public build review: no secrets or backend, local fonts/assets, no PDF/Blender/private source documents in dist
- NOT RUN: separate ESLint (not configured), real device integration or calibrated hydraulic tests (outside demonstrator scope), full browser suite, public deployment

## Limits and next action
- Source PDF does not contain manufacturing dimensions or certified electronics specifications. Geometry is a visual reconstruction and the farm is schematic
- Detection localizes the damaged branch between sensor pairs; exact crack coordinates, trained AI and real sensor communications are not implemented or claimed
- Completed authorized local platform; next changes should follow user feedback. Read `CODEX_HANDOFF.md` on a new session and reuse unchanged checks after impact analysis
